{
  "type": "module",
  "devDependencies": {
    "@types/node": "^20",
    "prettier": "^3",
    "prettier-plugin-organize-imports": "^3",
    "vite": "^4"
  },
  "scripts": {
    "dev": "bunx --bun vite",
    "build": "bunx --bun vite build",
    "check": "bunx --bun prettier . --check"
  }
}
