{
  "devDependencies": {
    "prettier": "^3",
    "vite": "^4"
  },
  "scripts": {
    "dev": "bunx --bun vite",
    "build": "bunx --bun vite build",
    "check": "bunx --bun prettier . --check"
  }
}
